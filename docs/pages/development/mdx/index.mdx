# Introduction

MDX is a superset of markdown. This means that all the markdown syntax still applies, e.g., creating bold text with `**bold**` or creating a link with `[link text](https://example.com)`.

## LaTeX formula

## Using ITELL-style UI components

```bash
pnpm add @itell/ui
```

```js
import { Component } from "@itell/ui";

<Component />;
```
