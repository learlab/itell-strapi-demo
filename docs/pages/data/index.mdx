# Data Storage

## Overview

ITELL collects data.

## Configure database

You can configure ITELL to use a different database by editing the `.env` file.

```bash filename=".env"
DATABASE_URL="your_database_connection_string"
```
